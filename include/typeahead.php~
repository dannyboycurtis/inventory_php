<?php 

$mysqli = inventory_db_connect();

if ( isset( $_POST['query'] ) and isset( $_POST['type'] ) )
{

	if ( $_POST['type'] == 'users' )
	{
		$query = '%' . $_POST['query'] . '%';
		$query_stmt = "SELECT CONCAT( f_name, ' ', l_name ) FROM user WHERE f_name LIKE ? OR l_name LIKE ?";

		if ( $stmt = $mysqli->prepare( $query_stmt ) )
		{
			$(

		}
	}
}

?>
